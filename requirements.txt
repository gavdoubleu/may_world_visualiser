Flask>=3.0.0
Flask-CORS>=4.0.0
h5py>=3.12.0
numpy>=2.0.0
pandas>=2.0.0
PyYAML>=6.0
numba>=0.61.0
pyproj>=3.4.0   # required when projection.type is 'utm' in app_config.yaml
orjson>=3.10.0  # world_map only — deliberately absent from pyproject.toml (world_reader stays dependency-minimal, ADR-0001)
